# Walkthrough Phase 15

- Đã chạy lệnh `php artisan make:migration add_profile_fields_to_users_table`.
- Đã bổ sung các trường: `phone`, `address`, `dob`, `gender`, `avatar`.
- Đã cấu hình seeder với dữ liệu mẫu cho user Nguyễn Quốc Thái.
- Đã tạo `ProfileController@index` và khai báo route.
- Đã xây dựng giao diện Profile (2 cột) theo chuẩn Liquid Glass V4 (backdrop-blur-3xl, ring-1, shadow-[...]).
- Đã migrate database và seed.
- Đã chạy npm run build.
